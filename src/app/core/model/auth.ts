export class Auth {
    username: string;
    // basicAuth?: string; // rimesso come token per il jwt
    expireSession: number;
    role: string;
    nome: string;
    cognome: string;
    token: string;
}
